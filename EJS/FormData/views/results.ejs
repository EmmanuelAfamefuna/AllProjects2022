<h1>Results Page</h1>

<p><a href="/">Form Page</a></p>

<% data.forEach(element => { %>
  <h4><%= element.fname%></h4>
  <h4><%= element.lname%></h4>
  <p><%= element.age%></p>
  <p><%= element.isAlive ? "ALIVE" : "DEAD!"%></p>
<% }) %>